# Método de instalación

# Dependencias

Es necesario instalar las siguientes librerías para que el programa funcione

```bash
pip install flask SQLAlchemy flask-cors python-dotenv Flask-SQLAlchemy jwt mysqlclient
```
